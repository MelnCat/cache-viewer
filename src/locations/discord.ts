import { CacheLocation } from "./location";

export const discord = new CacheLocation("Discord", async drive => {
	return [];
});
export const discordCanary = new CacheLocation("Discord Canary", async drive => {
	return [];
});